pre-commit:
  parallel: false
  commands:
    check:
      run: |
        export PATH="$HOME/.cargo/bin:$PATH"
        just check

pre-push:
  parallel: false
  commands:
    ci:
      run: |
        export PATH="$HOME/.cargo/bin:$PATH"
        just ci
